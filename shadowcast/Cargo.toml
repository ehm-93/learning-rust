[package]
name = "shadowcast"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16", features = ["vorbis", "wav"] }
bevy_rapier2d = "0.31"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
