[package]
name = "untitled"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.16"
avian2d = "0.3"
rand = "0.9"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
