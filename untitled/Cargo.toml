[package]
name = "untitled"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.16"
bevy_rapier2d = "0.31"
rand = "0.9"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
